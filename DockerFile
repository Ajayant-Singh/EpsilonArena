# Use a lightweight web server to serve the HTML/CSS/JS files
FROM nginx:alpine

# Copy all files from the current folder into Nginx's public directory
COPY . /usr/share/nginx/html

# Expose port 80 for the web traffic
EXPOSE 80